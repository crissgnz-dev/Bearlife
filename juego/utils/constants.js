export const TILE_SIZE = 32;
export const MAP_COLS = 80;
export const MAP_ROWS = 80;
export const fondoX = MAP_COLS * TILE_SIZE;
export const fondoY = MAP_ROWS * TILE_SIZE;

export const TERRAIN = {
  WATER: 0,
  LAND: 1,
  SAND: 2,
};
